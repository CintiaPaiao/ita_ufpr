# Implantação

MVP: SQLite + Streamlit. Piloto: ambiente controlado com dados sintéticos e pequena amostra validada. Produção: autenticação institucional, revisão LGPD, backups, banco persistente e avaliação de PostgreSQL. Docker: `docker compose up --build`.
